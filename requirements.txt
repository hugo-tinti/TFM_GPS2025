# ============================================================================
# REQUIREMENTS - Sistema GPS Profesional v4.3.7
# Última actualización: 31 Enero 2026
# ============================================================================

# Core Dash & Plotly (Framework principal)
dash>=2.14.0
dash-bootstrap-components>=1.5.0
plotly>=5.18.0

# Data Processing (Análisis de datos)
pandas>=2.0.0
numpy>=1.24.0

# Stats & ML (Tests estadísticos y machine learning)
scipy>=1.11.0
scikit-learn>=1.3.0

# File Handling (Manejo de archivos Excel)
openpyxl>=3.1.0

# Export (Exportación de gráficos a imágenes)
kaleido>=0.2.1

# Server & Optimization (Servidor y optimización)
Flask>=3.0.0
flask-caching>=2.1.0
gunicorn>=21.2.0

# PDF Generation (Opcional - solo si usas reportes PDF)
fpdf>=1.7.2

# IA & Variables de entorno
requests>=2.32.0
python-dotenv>=1.0.0

# Callbacks en segundo plano (opcional)
diskcache>=5.6.3

# Optimización de gráficos grandes (opcional)
datashader>=0.16.0
colorcet>=3.0.1


# ============================================================================
# NOTAS:
# - flask-caching mejora performance pero es opcional (hay fallback)
# - fpdf solo es necesario si generas reportes PDF
# - Flask-Login fue removido (no se usa en el script actual)
# - requests se usa para la API Groq (IA en VIZ 23)
# - python-dotenv se usa para leer GROQ_API_KEY desde .env
# ============================================================================
