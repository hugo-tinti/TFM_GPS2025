dash>=2.14.0
dash-bootstrap-components>=1.5.0
plotly>=5.18.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
openpyxl>=3.1.0
kaleido==0.2.1
Flask>=3.0.0
flask-caching>=2.1.0
gunicorn>=21.2.0
fpdf>=1.7.2
requests>=2.32.0
python-dotenv>=1.0.0
diskcache>=5.6.0
datashader>=0.15.0
groq>=0.4.0

# ============================================================================
# NOTAS:
# - flask-caching mejora performance pero es opcional (hay fallback)
# - fpdf solo es necesario si generas reportes PDF
# - Flask-Login fue removido (no se usa en el script actual)
# - requests se usa para la API Groq (IA en VIZ 23)
# - python-dotenv se usa para leer GROQ_API_KEY desde .env
# ============================================================================
